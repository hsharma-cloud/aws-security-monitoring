# VPC Flow Logs

VPC Flow Logs capture network traffic information for the VPC.

Configured destination: CloudWatch Logs.

Example observation:

External IP → EC2 instance → REJECT

This indicates internet scanners attempting to access the instance but being blocked.
